---
import { circleAnimation } from "../scripts/transitions";
---

<div class="page-cover" transition:animate={circleAnimation} />

<style>
    .page-cover {
        background-color: var(--background-1);
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 4;
        clip-path: circle(0% at 50% 50%);
        transition: clip-path 0.5s;
    }
</style>